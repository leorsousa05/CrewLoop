# Tasks: DiamondBlock as the Default Discovery Layer

- [x] Update `skills/crewloop-hub/SKILL.md` so discovery prefers DiamondBlock for memory, codebase search, and read-only exploration when configured.
- [x] Update `skills/diamondblock/SKILL.md` so the role explicitly includes codebase indexing/search.
- [x] Update `AGENTS.md`, `references/conventions.md`, and `references/workflow.md` to describe DiamondBlock as the default helper in the discovery flow.
- [x] Verify the fallback path still works when DiamondBlock is not installed or not configured.
- [x] Run `python3 scripts/validate-skills.py` after editing the SKILL.md files.
