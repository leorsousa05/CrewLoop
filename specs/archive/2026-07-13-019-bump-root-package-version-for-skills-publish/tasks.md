# Tasks: Root package version bump for skills publish

- [x] Bump the root `package.json` version so the published skills package can be released with the latest changes.
- [x] Update the root `package-lock.json` to match the new package version.
