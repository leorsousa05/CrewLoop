# Design

## Approach

1. Replace the simple core-flow diagram in `docs/docs/workflow.md` with a detailed end-to-end diagram.
2. Add sections explaining each phase, decision points, artifacts, and loops.
3. Keep the language clear and the diagram readable by splitting complex parts into subgraphs if needed.
4. Build and validate.

## Rollback

The change is documentation-only inside `docs/docs/workflow.md` and can be reverted with a single `git revert`.
