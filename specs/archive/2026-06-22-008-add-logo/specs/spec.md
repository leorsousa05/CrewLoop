# Spec: Add CrewLoop logo to Docusaurus site

## Acceptance criteria

1. The custom CrewLoop logo is served from `docs/static/img/logo.png`.
2. `docusaurus.config.js` references the new logo file.
3. `npm run build` succeeds.
