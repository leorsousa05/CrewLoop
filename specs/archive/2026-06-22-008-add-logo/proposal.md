# Proposal: Add CrewLoop logo to Docusaurus site

## Problem

The Docusaurus site currently uses the default Docusaurus logo. The project now has a custom CrewLoop logo that should be used in the navbar.

## Scope

In scope:
- Add the logo image to `docs/static/img/`.
- Update `docusaurus.config.js` to reference the new logo.
- Verify build succeeds.

Out of scope:
- Favicon generation.
- Social card redesign.
