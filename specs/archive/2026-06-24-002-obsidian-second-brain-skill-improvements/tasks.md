# Tasks: obsidian-second-brain Skill Improvements

## Phase 1 — Definitions and Navigation

- [ ] Add "Definitions" subsection after "MCP Tools Reference".
- [ ] Update final navigation menu to include `[D]` and `[S]`.
- [ ] Verify all internal links still work.

## Phase 2 — Layer Naming

- [ ] Replace `memory/` with `logs/` in architecture diagram.
- [ ] Replace `memory/` with `logs/` in decision tree.
- [ ] Replace `memory/` with `logs/` in layer semantics table.
- [ ] Replace `memory/` with `logs/` in heartbeat section.

## Phase 3 — Examples

- [ ] Add Example 6 for `Memory/` (user preference).
- [ ] Add Example 7 for `Notes/` (temporary scratchpad).
- [ ] Add Example 8 for `_Inbox/` (proposed canonical note).
- [ ] Verify every layer has at least one example.

## Phase 4 — Review

- [ ] Run `python scripts/validate-skills.py`.
- [ ] Read the updated skill end-to-end for consistency.
