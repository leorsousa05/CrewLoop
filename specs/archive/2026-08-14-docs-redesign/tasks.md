# Tasks

1. Create the new design token layer in `src/index.css` and `tailwind.config.js`.
2. Update the app shell in `src/App.tsx` so home/docs navigation matches the new visual direction.
3. Redesign `src/components/LandingPage.tsx` around the editorial hero and workflow spine.
4. Restyle `src/components/SkillVisualizer.tsx` to match the new handbook aesthetic.
5. Restyle `src/components/TerminalSimulator.tsx` to feel like a reference panel rather than a neon terminal.
6. Rebuild `src/components/DocsLayout.tsx` for the quieter reading desk layout.
7. Update `src/components/MarkdownRenderer.tsx` to apply the new typography, alert, table, and code styles.
8. Adjust `src/sidebarConfig.ts` only if the new layout requires category copy or ordering changes.
9. Update `src/components/tests/TerminalSimulator.test.tsx` if the simulator copy or timing changes.
10. Verify the redesigned app visually on desktop and mobile.
11. Check focus states, keyboard navigation, and reduced-motion behavior.
