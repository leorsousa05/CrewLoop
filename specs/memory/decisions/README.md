# decisions/

Lightweight rationale notes: `why-topic.md`.

Use when a choice is worth remembering but does not warrant a full ADR:

```markdown
# Why <choice>?

- Date:
- Decided by:
- Context:
- Alternatives rejected:
- Consequence:
```

Architecture-level decisions go to `specs/shared/adrs/` instead.
